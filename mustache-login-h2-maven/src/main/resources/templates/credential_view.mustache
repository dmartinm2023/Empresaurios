<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ver contraseña</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container">
    <div class="card">
      <h1>{{credential.title}}</h1>
      <p><strong>Usuario:</strong> {{credential.login}}</p>
      <p><strong>Contraseña:</strong> {{decrypted}}</p>

      {{#credential.onChainTxHash}}
      <hr>
      <p><strong>Huella on-chain:</strong></p>
      <p class="small"><code>{{credential.onChainCommitment}}</code></p>
      <p class="small">
        Tx: <a href="https://sepolia.etherscan.io/tx/{{credential.onChainTxHash}}" target="_blank" rel="noopener">
          {{credential.onChainTxHash}}
        </a>
      </p>
      {{/credential.onChainTxHash}}

      <p><a href="/">Volver</a></p>
    </div>
  </div>
</body>
</html>
